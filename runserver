#!/bin/sh

PWD=`pwd`
kitty --detach --title switchboard --directory $PWD ./run_switchboard
kitty --detach --title django --directory $PWD ./run_django
sleep 4s && kitty --detach --title celery --directory $PWD ./run_celery
